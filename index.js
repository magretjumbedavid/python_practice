function near(distances){
for (let i=0;i<distances.length; i++){
    if(distances[i]<=5){
      console.log("The driver is available");
}
    
        
    else{
        console.log("The driver is not available");
    }

}


}
const distances=([3,4,5,8,9,10]);
near(distances);

class order{
    constructor(orderId ,items){
        this.orderId=orderId;
        this.items=items;
    }
}
calculateTotalPrinces.prototype.order= function(){
    Price.reduce((a,b) => 0,a+b)
    return totalprice
}
paymentProcess.prototype.order=function(){
    return "The process is in progress within two seconds"
}